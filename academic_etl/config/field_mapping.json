{
  "comment": "Mapping config for University_Import_Clean CSV. The file's header row is misaligned vs its data rows (data rows lack the 'id' value and sometimes the leading blank column), so rows are re-aligned per-row using anchor fields instead of trusting the header. canonical_fields is the true left-to-right order of values in a data row.",
  "canonical_fields": [
    "name",
    "location",
    "description",
    "slug",
    "sponsored",
    "website",
    "global_rank",
    "financials",
    "student_loan_available",
    "campus_student_life",
    "number_of_students",
    "student_to_faculty_ratio",
    "international_student_ratio",
    "housing_availability",
    "admissions_contact",
    "admissions_phone",
    "contact_person",
    "admissions_page_link",
    "immigration_support",
    "university_campuses"
  ],
  "anchor_fields": {
    "website": { "index": 5, "pattern": "^https?://" },
    "slug": { "index": 3, "pattern": "^[a-z0-9]+(-[a-z0-9]+)+$" }
  }
}
